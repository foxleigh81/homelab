apiVersion: v1
kind: Service
metadata:
  name: home-assistant
  namespace: arctic
spec:
  type: LoadBalancer
  selector:
    app: home-assistant
  ports:
    - protocol: TCP
      port: 8123 # Exposed port
      targetPort: 8123 # Port inside the Pod